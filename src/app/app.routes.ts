import { Component } from '@angular/core';
import { Routes } from '@angular/router';
import { WeatherwebComponent } from './weatherweb/weatherweb.component';

export const routes: Routes = [
   { path:'',component:WeatherwebComponent}
];
